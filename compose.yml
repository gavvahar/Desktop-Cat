services:
  web:
    build: .
    image: desktopcat-site:latest
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - cache-data:/tmp/desktopcat-site-cache

volumes:
  cache-data:
