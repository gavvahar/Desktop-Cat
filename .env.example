# Copy to .env and fill in for real deployment. docker-compose.yml loads
# this file automatically.

# Long random string -- e.g. `python -c "import secrets; print(secrets.token_urlsafe(50))"`
DJANGO_SECRET_KEY=change-me-to-a-long-random-string

DJANGO_DEBUG=false

# Comma-separated. Must include whatever domain this is actually served on.
DJANGO_ALLOWED_HOSTS=downloads.example.com

GITHUB_REPO=gavvahar/Desktop-Cat

# Optional: a token raises the GitHub API rate limit from 60/hr to 5000/hr
# per the server's IP (shared across all visitors, since fetches happen
# server-side). Not needed at low traffic -- release data is cached for 5
# minutes regardless. No scopes required, this only reads public releases.
GITHUB_TOKEN=
